<p id="notice"><%= notice %></p>

<h1>Flats</h1>
<div class="row">
  <% @flats.each do |flat| %>
    <div class="col-4 mt-4">
      <div class="card-trip">
        <img src="http://lorempixel.com/400/200/" />
        <div class="card-trip-infos">
          <div>
            <h2><%= flat.name %></h2>
            <p><%= flat.description %></p>
          </div>
          <div><h2 class="card-trip-pricing"><%= flat.price_per_night %>¢</h2>
          <h2 class="card-trip-pricing"><%= flat.number_of_guests %> <i class="far fa-user"></i></h2></div>
          <div>
            <%= link_to 'Show', flat, :class => 'm-1 ml-3 btn-primary mw-100' %>
            <%= link_to 'Edit', edit_flat_path(flat), :class => "m-1 ml-3  btn btn-warning btn-block" %>
            <%= link_to 'Destroy', flat, :class => "m-1 ml-3 btn btn-danger btn-block", method: :delete, data: { confirm: 'Are you sure?' } %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>


<br>

<%= link_to 'New Flat', new_flat_path, :class => 'btn btn-success' %>
